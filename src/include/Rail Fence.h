#ifndef RAIL FENCE_H
#define RAIL FENCE_H
#include <string>

std::string cifrar_railfence(int clave, std::string mensaje);
std::string descifrar_railfence(int clave, std::string mensaje);

#endif // RAIL FENCE_H
