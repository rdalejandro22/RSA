#include "Cesar.h"
/*Cesar::Cesar(int clave)
{
    this->clave=clave;
}
string Cesar::cifrar(string mensaje)
{
    for(int i=0;i<mensaje.length();i++)
    {
        int posicion = alfabeto.find(mensaje[i]);
        if(posicion+clave>alfabeto.length())
        {
            posicion = modulo(posicion+clave,alfabeto.length());
            mensaje[i] = alfabeto[posicion];
            continue;
        }
        mensaje[i] = alfabeto[posicion+clave];
    }
    return mensaje;
}
string Cesar::descifrar(string mensaje_cifrado)
{
    for(int i=0;i<mensaje_cifrado.length();i++)
    {
        int posicion = alfabeto.find(mensaje_cifrado[i]);
        if(posicion-clave<0)
        {
            posicion = modulo(posicion-clave,alfabeto.length());
            mensaje_cifrado[i] = alfabeto[posicion];
            continue;
        }
            mensaje_cifrado[i] = alfabeto[posicion-clave];
    }
    return mensaje_cifrado;
}*/
